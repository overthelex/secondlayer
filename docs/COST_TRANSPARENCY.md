# Прозрачность стоимости в SecondLayer MCP Server

## Обзор

SecondLayer MCP Server предоставляет **полную прозрачность стоимости** для всех операций. Пользователи видят примерную стоимость каждого инструмента ДО его использования в описании tool.

## Как это работает

### 1. Информация о стоимости в tool descriptions

Каждый инструмент содержит в описании:
- 💰 Примерную стоимость в USD
- Пояснение из чего складывается стоимость
- Диапазон цен в зависимости от параметров

### 2. Фактическая стоимость в ответе

После выполнения запроса, в ответе возвращается точная стоимость:
```json
{
  "result": { /* ... результаты ... */ },
  "cost_tracking": {
    "request_id": "uuid",
    "estimate_before": { /* оценка до выполнения */ },
    "actual_cost": {
      "openai": { "total_cost_usd": 0.000152 },
      "zakononline": { "total_cost_usd": 0.007140 },
      "secondlayer": { "total_cost_usd": 0.035700 },
      "totals": { "cost_usd": 0.042992 }
    }
  }
}
```

## Стоимость по инструментам

### 🔍 search_legal_precedents
**Примерная стоимость:** $0.03-$0.10 USD

Включает:
- OpenAI API (embeddings)
- ZakonOnline API (поиск судебных решений)
- SecondLayer MCP (обработка документов)

**Когда дороже:**
- Большое количество результатов (limit > 10)
- Сложные семантические запросы
- Поиск по номеру дела (требует обработки исходного документа)

### 📊 analyze_case_pattern
**Примерная стоимость:** $0.02-$0.08 USD

Включает:
- OpenAI API (анализ паттернов)
- PostgreSQL запросы

**Когда дороже:**
- Большое количество case_ids для анализа
- Комплексный intent требующий глубокого анализа

### 🔎 get_similar_reasoning
**Примерная стоимость:** $0.01-$0.03 USD

Включает:
- OpenAI API (embeddings)
- Qdrant векторный поиск

**Минимальная стоимость** - использует только векторный поиск

### 📄 extract_document_sections
**Примерная стоимость:** $0.005-$0.05 USD

Включает:
- При `use_llm=false`: только HTML парсинг (почти бесплатно)
- При `use_llm=true`: OpenAI API для точной экстракции

**Рекомендация:** Используйте `use_llm=false` для первичной обработки

### 📚 find_relevant_law_articles
**Примерная стоимость:** $0.01-$0.02 USD

Включает:
- PostgreSQL запросы к legal patterns

**Минимальная стоимость** - только запросы к БД

### ✅ check_precedent_status
**Примерная стоимость:** $0.005-$0.015 USD

Включает:
- PostgreSQL запросы

**Минимальная стоимость** - только запросы к БД

### 🕸️ get_citation_graph
**Примерная стоимость:** $0.005-$0.02 USD

Включает:
- PostgreSQL запросы

**Когда дороже:**
- Большая глубина графа (depth > 2)

### ⚖️ get_legal_advice (самый дорогой)
**Примерная стоимость:** $0.10-$0.30 USD

Включает:
- Множественные вызовы OpenAI API
- ZakonOnline API
- SecondLayer MCP (обработка множества документов)
- Hallucination Guard (проверка источников)

**Зависит от reasoning_budget:**
- `quick`: ~$0.10 (базовый анализ)
- `standard`: ~$0.15-$0.20 ✅ **Рекомендуется**
- `deep`: ~$0.25-$0.30 (глубокий анализ)

## Как пользователи видят стоимость

### В ChatGPT/Claude Desktop

При просмотре доступных инструментов:

```
🔧 Доступные инструменты:

📍 search_legal_precedents
   Поиск юридических прецедентов с семантическим анализом

   💰 Примерная стоимость: $0.03-$0.10 USD
   Стоимость зависит от сложности запроса и количества результатов.
   Включает OpenAI API (embeddings), ZakonOnline API (поиск),
   SecondLayer MCP (обработка документов).

📍 get_legal_advice
   Главный инструмент: комплексный юридический анализ ситуации
   с проверкой источников и детекцией галлюцинаций

   💰 Примерная стоимость: $0.10-$0.30 USD (зависит от reasoning_budget)
   • quick: ~$0.10 (базовый анализ)
   • standard: ~$0.15-$0.20 (рекомендуется)
   • deep: ~$0.25-$0.30 (глубокий анализ с проверкой всех источников)

   Самый дорогой инструмент. Включает множественные вызовы
   OpenAI API, ZakonOnline API, SecondLayer MCP и проверку галлюцинаций.
```

### Перед выполнением

AI модель видит стоимость и может:
- Предупредить пользователя о дорогой операции
- Предложить более дешевую альтернативу
- Выбрать оптимальный reasoning_budget

**Пример диалога:**

```
User: Найди мне все дела по мобілізації за 2024 год

AI: Я могу использовать инструмент search_legal_precedents для этого запроса.
    Примерная стоимость: $0.05-$0.08. Продолжить?

User: Да

AI: [вызывает search_legal_precedents]
```

### После выполнения

В логах сервера и в ответе API:

```
info: Cost tracking completed {
  requestId: "abc-123",
  status: "completed",
  totalCostUsd: "0.042992"
}
```

## Компоненты стоимости

### OpenAI API
- **Embeddings** (text-embedding-ada-002): $0.10 per 1M tokens
- **Chat** (gpt-4o-mini): $0.15 input, $0.60 output per 1M tokens
- **Chat** (gpt-4o): $2.50 input, $10.00 output per 1M tokens

### ZakonOnline API
Прогрессивные тарифы:
- 0-10,000 вызовов/месяц: $0.00714 per call
- 10,001-20,000: $0.00690 per call
- 20,001-30,000: $0.00667 per call
- 30,001-50,000: $0.00643 per call
- 50,001+: $0.00238 per call

### SecondLayer MCP API
Те же тарифы что и ZakonOnline:
- Веб-скрапинг документов
- Обработка HTML
- Извлечение текста

**Кешированные запросы:** бесплатно ✅

## Оптимизация затрат

### 1. Используйте кеш
Повторные запросы к тем же документам - бесплатно

### 2. Выбирайте правильный reasoning_budget
- `quick` для простых вопросов
- `standard` для большинства случаев
- `deep` только когда нужен глубокий анализ

### 3. Ограничивайте limit
```json
{
  "query": "мобілізація",
  "limit": 5  // вместо 10
}
```

### 4. Используйте специализированные tools
Вместо `get_legal_advice` используйте:
- `search_legal_precedents` - только для поиска
- `analyze_case_pattern` - только для анализа паттернов
- `extract_document_sections` - только для экстракции секций

### 5. Используйте use_llm=false для extract_document_sections
HTML парсинг почти бесплатен

## Мониторинг затрат

### База данных
Все затраты сохраняются в PostgreSQL:

```sql
-- Просмотр последних 10 запросов
SELECT
  tool_name,
  total_cost_usd,
  execution_time_ms,
  created_at
FROM cost_tracking
ORDER BY created_at DESC
LIMIT 10;

-- Статистика по месяцам
SELECT
  year_month,
  zakononline_total_calls,
  zakononline_total_cost_usd,
  secondlayer_total_calls,
  secondlayer_total_cost_usd
FROM monthly_api_usage
ORDER BY year_month DESC;
```

### HTTP API
```bash
# Получить статистику затрат (TODO: добавить endpoint)
GET /api/cost-stats?period=month
```

## Будущие улучшения

### Планируется
- [ ] Админ-панель для просмотра статистики затрат
- [ ] REST endpoint `/api/cost-stats` для получения статистики
- [ ] Email уведомления при превышении лимитов
- [ ] Квоты и лимиты на пользователя/ключ
- [ ] Детальная breakdown по каждому API call

### Не планируется
- ❌ Запрос подтверждения перед каждым вызовом (слишком навязчиво)
- ❌ Блокировка дорогих операций (доверяем пользователю)
- ❌ Prepaid модель (пока работаем на trust-based модели)

## Политика прозрачности

SecondLayer следует принципу **полной прозрачности затрат**:

✅ Пользователь ВСЕГДА видит примерную стоимость ДО выполнения
✅ Фактическая стоимость ВСЕГДА возвращается в ответе
✅ Все затраты ВСЕГДА сохраняются в базе данных
✅ Кешированные запросы ВСЕГДА бесплатны

Мы верим что информированные пользователи делают лучший выбор.

---

**Обновлено:** 2026-01-17
**См. также:**
- [pricing.json](../pricing.json) - детальные тарифы
- [cost-tracker.ts](../mcp_backend/src/services/cost-tracker.ts) - реализация
