httpx>=0.27,<0.29
# httpx does not speak SOCKS on its own: a socks5h:// proxy URL raises
# ImportError at AsyncClient construction without this. Needed because
# amtsblattportal.ch does not answer AWS IPs, so the SHAB stages fetch
# through a reverse SOCKS tunnel (CHPIPE_SHAB_PROXY).
socksio>=1.0
psycopg[binary]>=3.1,<4
lxml>=5.2,<6
# The CH point-in-time benchmark's scorer, templates, report and core split
# (chpipe.bench.build / run_oracle import them). Source of truth:
# https://github.com/overthelex/ch-pit -- pin the tag the build was made with.
chpit @ git+https://github.com/overthelex/ch-pit.git@v2026.9.0
pytest>=8.2,<9
pytest-asyncio>=0.23,<0.25
