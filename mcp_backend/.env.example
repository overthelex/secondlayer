# Database
DATABASE_URL=postgresql://secondlayer:secondlayer_password@localhost:5432/secondlayer_db

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Qdrant Vector Database
QDRANT_URL=http://localhost:6333

# ============================================
# OpenAI Configuration
# ============================================

# Option 1: Use different models for different complexity levels (RECOMMENDED)
# This optimizes cost vs quality based on task complexity
OPENAI_MODEL_QUICK=gpt-4o-mini       # Fast, cheap - simple tasks (keyword extraction, quick classification)
OPENAI_MODEL_STANDARD=gpt-4o-mini    # Balanced - most tasks (intent classification, moderate analysis)
OPENAI_MODEL_DEEP=gpt-4o             # Best quality - complex tasks (deep section extraction, complex reasoning)

# Option 2: Use single model for all tasks (simpler but less cost-efficient)
# If set, this overrides the specific models above
# OPENAI_MODEL=gpt-4o

# Embedding model (must stay consistent once you have vectors in Qdrant!)
# Options: text-embedding-ada-002 (default), text-embedding-3-small, text-embedding-3-large
OPENAI_EMBEDDING_MODEL=text-embedding-ada-002

# API Keys (supports rotation for rate limiting)
OPENAI_API_KEY=sk-...
OPENAI_API_KEY2=sk-...  # Optional fallback key

# ============================================
# Zakononline API
# ============================================
ZAKONONLINE_API_TOKEN=your-token
ZAKONONLINE_API_TOKEN2=your-token-2  # Optional fallback

# ============================================
# HTTP Server Security
# ============================================
SECONDARY_LAYER_KEYS=test-key-123,dev-key-456
HTTP_PORT=3000
HTTP_HOST=0.0.0.0

# ============================================
# Node Environment
# ============================================
NODE_ENV=development
