apiVersion: 1

datasources:
  - name: Prometheus
    uid: Prometheus
    type: prometheus
    access: proxy
    url: http://$PROMETHEUS_HOST:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 10s
