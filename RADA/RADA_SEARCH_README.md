# 🇺🇦 Пошук на порталі Верховної Ради України

Node.js скрипт для пошуку та завантаження даних з порталів Верховної Ради:

- **data.rada.gov.ua** — відкриті дані (депутати, голосування, законопроекти)
- **zakon.rada.gov.ua** — законодавство України (закони, кодекси, Конституція)

## 📦 Встановлення

```bash
# Залежності не потрібні (використовує вбудовані модулі Node.js)
chmod +x fetch_rada_dataset.js
```

## 🚀 Швидкий старт

### Отримати Конституцію України

```bash
node fetch_rada_dataset.js --law constitution
# або
node fetch_rada_dataset.js --law "254к/96-вр"
# зберегти у файл
node fetch_rada_dataset.js --law constitution --output constitution.html
```

### Пошук документів

```bash
node fetch_rada_dataset.js --search "цивільний кодекс"
node fetch_rada_dataset.js --search "про освіту"
node fetch_rada_dataset.js --search "земельний"
```

### Список відкритих даних

```bash
node fetch_rada_dataset.js --list
```

## 📚 Команди

| Команда | Опис | Приклад |
|---------|------|---------|
| `--list`, `-l` | Список наборів відкритих даних | `node fetch_rada_dataset.js --list` |
| `--search`, `-s` | Пошук у законодавстві | `node fetch_rada_dataset.js -s "конституція"` |
| `--law` | Отримати закон за номером | `node fetch_rada_dataset.js --law 254к/96-вр` |
| `<dataset>` | Завантажити набір даних | `node fetch_rada_dataset.js mps` |

## ⚙️ Опції

| Опція | Опис | Приклад |
|-------|------|---------|
| `--output`, `-o` | Зберегти у файл | `--output result.json` |
| `--format` | Формат: json, csv, xml | `--format csv` |
| `--file` | Конкретний файл з набору | `--file deputies.json` |
| `--help`, `-h` | Показати довідку | `--help` |

## 📜 Відомі документи (псевдоніми)

Можна використовувати замість офіційних номерів:

| Псевдонім | Документ | Номер |
|-----------|----------|-------|
| `constitution`, `конституція` | Конституція України | 254к/96-вр |
| `цивільний кодекс` | Цивільний кодекс України | 435-15 |
| `кримінальний кодекс` | Кримінальний кодекс України | 2341-14 |
| `сімейний кодекс` | Сімейний кодекс України | 2947-14 |
| `господарський кодекс` | Господарський кодекс України | 436-15 |
| `кпк` | Кримінальний процесуальний кодекс | 4651-17 |
| `цпк` | Цивільний процесуальний кодекс | 1618-15 |

## 📂 Набори відкритих даних

Основні набори на data.rada.gov.ua:

| ID | Назва | Опис |
|----|-------|------|
| `mps` | Народні депутати | Інформація про депутатів та їх активність |
| `zpr` | Законопроекти | Зареєстровані законопроекти |
| `zal` | Пленарні засідання | Голосування, виступи, порядок денний |
| `zak` | Законодавство | База даних "Законодавство України" |
| `meetings` | Засідання | Стенограми, порядок денний |
| `fin` | Фінанси | Господарсько-фінансова діяльність |
| `aut` | Структура | Організаційна структура ВРУ |

## 💡 Приклади використання

### Завантажити список депутатів

```bash
node fetch_rada_dataset.js mps --output deputies.json
```

### Пошук законів про освіту

```bash
node fetch_rada_dataset.js --search "про освіту"
```

### Отримати Кримінальний кодекс

```bash
node fetch_rada_dataset.js --law "кримінальний кодекс" --output kk.html
```

### Завантажити законопроекти у форматі CSV

```bash
node fetch_rada_dataset.js zpr --format csv --output bills.csv
```

## 🔗 Корисні посилання

- **Портал відкритих даних:** https://data.rada.gov.ua
- **Законодавство України:** https://zakon.rada.gov.ua
- **API документація:** https://data.rada.gov.ua/ogd/zak/laws/

## 📋 Структура відповіді пошуку

При пошуку документів (`--search`) скрипт повертає:
- Назву документа
- Унікальний ID
- Пряме посилання на документ

При отриманні закону (`--law`) скрипт показує:
- Повну назву
- Дату прийняття
- Кількість статей
- Посилання на картку та текст
- Попередній перегляд тексту

## ⚠️ Примітки

1. **Кодування:** Сервер використовує UTF-8 для JSON та HTML
2. **Стиснення:** Скрипт автоматично обробляє gzip, deflate, brotli
3. **Rate limiting:** Рекомендується робити паузи між запитами при масовому завантаженні
4. **Редакції:** За замовчуванням завантажується поточна редакція документа

## 🐛 Вирішення проблем

**Помилка "Registry is not an array":**
- API змінило формат — скрипт вже оновлено для підтримки нового формату

**Помилка "HTTP 404":**
- Перевірте правильність номера документа
- Спробуйте пошук: `--search "назва документа"`

**Проблеми з кодуванням:**
- Переконайтеся, що термінал підтримує UTF-8
- Використовуйте `--output` для збереження у файл

---

*Останнє оновлення: Січень 2026*
